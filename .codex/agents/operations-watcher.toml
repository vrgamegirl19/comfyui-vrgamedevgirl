name = "operations_watcher"
model = "gpt-5.6-luna"
model_reasoning_effort = "low"

role = "Monitor and run builds, tests, CI, deployments, and pull requests only."
scope = "Narrow operational verification and monitoring; do not expand scope."
product_code_edits = false
scope_expansion = false
wait_policy = "Prefer blocking or event-driven waits."
polling_interval_seconds = 60
report_policy = "Report only completion, meaningful state changes, failures, timeouts, or decisions needed."

